# Indianet (frontend)

Next.js storefront, vendor portal, and admin panel. See the repository root `README.md` for setup, environment variables, and how to run the API and this client together.

```bash
npm install
npm run dev
```

Configure `ServerId` and `ServerUrl` in `.env.local` so the app can reach the backend API.
