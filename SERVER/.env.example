PORT=5000
DB_URL=mongodb://127.0.0.1:27017
DB_NAME=indianet
JWT_SECRET=local-dev-jwt-secret-change-before-production

# Optional: vendor pickup is registered when set; acceptance still works without these
SHIPROCKET_EMAIL=
SHIPROCKET_PASS=
SHIPROCKET_PICKUPID=Delhi
MAIL_USER=
MAIL_PASS=
MAIL_FROM="Indianet <noreply@localhost>"
ADMIN_MAIL=
# Dashboard login (seeded into MongoDB `admin` collection on server start)
ADMIN_EMAIL=
ADMIN_PASSWORD=
RAZORPAY_ID=
RAZORPAY_SECREt=
